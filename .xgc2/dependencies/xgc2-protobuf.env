# Exact capability-first protocol product adopted by this Runtime SDK release.
: "${XGC2_PROTOBUF_VERSION:=0.5.0-1}"
# Replaced with the immutable 0.5 release commit by the release train. Source
# builds must pass XGC2_PROTOBUF_SOURCE_REF explicitly until that commit exists.
: "${XGC2_PROTOBUF_SOURCE_REF:=UNRELEASED_0_5_0}"
